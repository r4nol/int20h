name: "CI — Frontend"

on:
  push:
    branches: [main]
    paths:
      - "src/frontend/**"
      - ".github/workflows/ci-frontend.yaml"
  pull_request:
    branches: [main]
    paths:
      - "src/frontend/**"
      - ".github/workflows/ci-frontend.yaml"

# Cancel in-progress runs for the same branch/PR to save CI minutes
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  packages: write        # Push to GHCR
  id-token: write        # Request OIDC JWT (for k8s auth in deploy workflow)
  security-events: write # Upload Trivy SARIF to GitHub Security tab

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository_owner }}/frontend

jobs:
  build-scan-push:
    name: "Build → Scan → Push"
    runs-on: ubuntu-latest
    outputs:
      image-tag: ${{ steps.meta.outputs.version }}
      image-digest: ${{ steps.build.outputs.digest }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Generate Docker image tags based on git context:
      #   PR:   pr-42-abc1234
      #   main: main-abc1234 + latest
      #   tag:  v1.2.3
      - name: Docker Metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha,prefix=main-,format=short,event=push
            type=ref,event=pr,suffix=-${{ github.sha }}
            type=semver,pattern={{version}}
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}
          labels: |
            org.opencontainers.image.title=Online Boutique Frontend
            org.opencontainers.image.description=Online Boutique frontend microservice
            org.opencontainers.image.vendor=INT20H Hackathon

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # Login only on non-PR events (PRs from forks can't access secrets)
      - name: Login to GHCR
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Multi-arch build: linux/amd64 + linux/arm64
      # BuildKit layer cache stored in GitHub Actions cache (free)
      - name: Build and Push
        id: build
        uses: docker/build-push-action@v5
        with:
          context: src/frontend
          platforms: linux/amd64,linux/arm64
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: true    # SBOM provenance attestation

      # Trivy security scan — reports CRITICAL/HIGH CVEs to GitHub Security tab
      # continue-on-error: pipeline continues even if unfixed vulns are found
      # ignore-unfixed=true: skip CVEs with no patch available (reduces noise)
      - name: Trivy Vulnerability Scan
        id: trivy
        continue-on-error: true
        uses: aquasecurity/trivy-action@0.30.0
        with:
          image-ref: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ steps.meta.outputs.version }}
          format: sarif
          output: trivy-frontend.sarif
          severity: CRITICAL,HIGH
          ignore-unfixed: true
          exit-code: "1"
        env:
          TRIVY_USERNAME: ${{ github.actor }}
          TRIVY_PASSWORD: ${{ secrets.GITHUB_TOKEN }}

      # Upload results to GitHub Security tab — only if the SARIF file was created
      # (Trivy may fail before writing output if the image is inaccessible)
      - name: Upload Trivy SARIF to GitHub Security
        uses: github/codeql-action/upload-sarif@v4
        if: always() && hashFiles('trivy-frontend.sarif') != ''
        with:
          sarif_file: trivy-frontend.sarif
          category: trivy-frontend

      - name: Build Summary
        if: always()
        run: |
          echo "## Frontend CI Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| | |" >> $GITHUB_STEP_SUMMARY
          echo "|---|---|" >> $GITHUB_STEP_SUMMARY
          echo "| Image | \`${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ steps.meta.outputs.version }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| Digest | \`${{ steps.build.outputs.digest }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| Platforms | linux/amd64, linux/arm64 |" >> $GITHUB_STEP_SUMMARY
          echo "| Trivy | ${{ steps.trivy.outcome }} |" >> $GITHUB_STEP_SUMMARY

  # Trigger staging deployment after successful build on main branch only
  deploy-staging:
    name: "Deploy → Staging"
    needs: build-scan-push
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    uses: ./.github/workflows/deploy.yaml
    with:
      service: frontend
      image-tag: ${{ needs.build-scan-push.outputs.image-tag }}
      environment: staging
    secrets: inherit
    permissions:
      contents: write
      id-token: write
